<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DeepLink">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.DeepLink">

            <!-- 기본 런처 인텐트 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep Link 인텐트 필터: 커스텀 스킴 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- myshop://product/{productId} -->
                <data
                    android:scheme="myshop"
                    android:host="product" />
            </intent-filter>

            <!-- Deep Link 인텐트 필터: HTTPS (App Links) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- https://myshop.example.com/product/{productId} -->
                <data
                    android:scheme="https"
                    android:host="myshop.example.com"
                    android:pathPrefix="/product" />
            </intent-filter>

            <!-- Deep Link 인텐트 필터: 장바구니 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- myshop://cart -->
                <data
                    android:scheme="myshop"
                    android:host="cart" />
            </intent-filter>

        </activity>
    </application>

</manifest>
